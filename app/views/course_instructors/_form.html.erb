<%= form_with(model: course_instructor, local: true) do |form| %>
  <% if course_instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_instructor.errors.count, "error") %> prohibited this course_instructor from being saved:</h2>

      <ul>
      <% course_instructor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.collection_select :course_id, Course.all, :id, :title %>
  </div>

  <div class="field">
    <%= form.label :instructor_id %>
    <%= form.collection_select :instructor_id, Instructor.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
