<div class="field">
      <%= f.label :description %> 
      <%= f.text_area :description, cols: 60, rows:3 %>
      <%= f.label :marks %>
      <%= f.text_field :marks %>
</div>

<%= f.fields_for :choices do |builder| %>	
  <%= render 'choice_fields', f: builder %>
<% end %>
